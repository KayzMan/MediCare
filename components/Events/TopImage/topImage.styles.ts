import { StyleSheet } from "react-native";

// 👇 theme
import { theme } from "../../../theme";

export const topImageStyles = StyleSheet.create({
  container: {
    marginBottom: theme.sizes.appMargin,
  },
});
